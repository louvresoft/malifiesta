import { PricingConversionPipe } from './pricing-conversion.pipe';

describe('PricingConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new PricingConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
